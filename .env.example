# MathCamp Environment Variables

# PostHog Analytics Configuration (Optional)
# Get your API key from https://posthog.com/
# Leave blank to run in local-only mode (no cloud analytics)
VITE_POSTHOG_API_KEY=
VITE_POSTHOG_HOST=https://us.i.posthog.com

# Note: All child learning data stays local regardless of PostHog settings
# PostHog only receives anonymous usage metrics (MAU, feature usage, etc.)
